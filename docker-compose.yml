services:
  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ./ollama_data:/root/.ollama

  backend-ai:
    build: ./backend
    ports:
      - "8000:8000"
    depends_on:
      - ollama